name: modular
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  drift:

dev_dependencies:
  build_runner: ^2.3.2
  drift_dev:
  lints: any # resolved by workspace, updated globally
  test: ^1.25.14
