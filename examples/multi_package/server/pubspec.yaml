name: server
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.4.0
  drift: ^2.15.0
  drift_postgres: ^1.1.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0

  shared:
    path: ../shared/
  postgres: ^3.0.8

dev_dependencies:
  build_runner: ^2.4.8
  http: ^1.1.0
  lints: any # resolved by workspace, updated globally
  test: ^1.24.0
  drift_dev: ^2.15.0
