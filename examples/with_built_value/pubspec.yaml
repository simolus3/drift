name: with_built_value
publish_to: none
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  drift: ^2.0.0-dev
  built_value: ^8.0.0

dev_dependencies:
  drift_dev: ^2.0.0-dev
  built_value_generator: ^8.1.1
  build_runner: ^2.0.0

dependency_overrides:
  drift:
    path: ../../drift
  drift_dev:
    path: ../../drift_dev
  sqlparser:
    path: ../../sqlparser
